#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
if [ ! -x .venv/bin/python ]; then
  echo "Create the local Python environment first; see README setup."
  exit 1
fi
exec .venv/bin/python scripts/dev.py "$@"
